apiVersion: v1
kind: Service
metadata:
  name: {{ include "aof-back.fullname" . }}
  labels:
    {{- include "aof-back.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "aof-back.selectorLabels" . | nindent 4 }}
